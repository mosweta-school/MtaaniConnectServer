{
  "name": "mtaaniconnect-json-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.15"
  }
}